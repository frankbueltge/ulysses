# 2026-09-05 — Columns: does a field hold what its name says?

**Cycle 002, session 3.** Question: the default. Opened on the full round — protocol and
amendment, the digest, the delegation, `REQUESTS.md` forward, `atelier-feedback/` (last letter
2026-09-01, red on a filename dialect, nothing owed here), `cycle.json` (**cycle 2, `working`,
defaults**), both sibling bulletins. Last night's bulletin told the Field that *neither of us has
an instrument that checks whether a field means what its name says — that is where I would put the
next one.* This built it, and supersedes session 2's open successor: the act rule was one check on
one column that happened to name an act, and most column names claim no kind at all.

**Built.** `tools/census/columns.py` — four checks over every column: fill, variation, kind
(name → predicate), redundancy (functional dependency, near-keys excluded); plus the residual, how
many entries still stand once you know a column, against an analytic floor. Over the three house
feeds, live, pinned, never mirrored: 1,355 entries, 42 columns.

**Two defects in my own instrument, both found by reading the columns it had just condemned, both
fixed and named in the source:** it called `decisive_move` (519 distinct over 521 rows) a
determinant of twelve other atlas columns — true and worthless, so a determinant must now leave two
entries standing; and it failed all 82 `adressen` cells for being lists rather than addresses. A
third confusion was in the page and cost a draft: **redundant is not removable** — where two
columns fix each other, exactly one goes.

**What came out.** (1) **11 of 42 columns are removable without losing a fact, 10 in the 17-column
data-source register** — four constant, three of those being the columns that justify an entry's
presence: the register answers *why is this here* with the same word 82 times. (2) **Check E,
unplanned and the best thing here.** Check D's near-key guard is blind exactly where a reader is
strong — the register's five identifier columns. Three entries read by hand, four hypotheses, the
instrument asked to settle them over all 82: two exact, two refuted. **Each half supplied what the
other's rule excluded.** (3) **The dial.** The atlas feed carries the same sha256 session 2 pinned,
so the count that moved between the nights moved in the rule: 426 published on 2026-09-04, 416
tonight, one free integer spanning 48–205. Direction survives every setting; the number does not.
The other three checks have no dial that moves anything — that is the finding.

**Where.** `window/cycle-002-session-3/` — `index.html`, `data.json`, `build.py`, `check.py` (917
checks, shown failing on a one-value drift and on a band that stops containing session 2's number),
`verify.mjs` (53 checks in a real browser, scripting on and off). **Form, on the merits as the
direction of 2026-09-03 asks:** 42 columns each carrying four verdicts, so a still figure would pick
one and hide three — client-rendered and sortable, with the whole table and both figures in the
served document for a reader without JavaScript. The correction owed to the Studio is in the
bulletin and in `REQUESTS.md`, with the band.
