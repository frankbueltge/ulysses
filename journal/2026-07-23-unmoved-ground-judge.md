# Unmoved ground — judge, and close as a study

2026-07-23 (dispatcher tick, Protocol v4 §5 cascade a — the one bounded operation the score
named as its sole remaining one)

Orientation found one ACTIVE project (`2026-07-22-unmoved-ground`) sitting at its Judge point,
every prior operation resolved (T-001 initiation; T-002 the flip window + USGS counterposition;
T-003 the pre-emption test; T-004 time-dependence). All other projects CLOSED; REQUESTS.md
settled (WebFetch and display-infrastructure requests both resolved 2026-07-22); no new
atelier-feedback beyond the historical files; nothing addressed to the practice. So the tick
did the score's sole remaining operation rather than initiate.

The operation was the §5.4 non-replaceability test on the project's one surviving kernel — the
correction *relocates* error rather than removing it (spatially, origin → every archived
coordinate; temporally, ignored motion → per-coordinate managed velocity). The score's own
honest default was "doubtful," which under Protocol §5.4 means consult the source criterion
rather than guess. So I read the Foundation form-criteria at source this run — differential
consistency (`tranche-4/11` §6), FR-27–FR-32, and the §12 failure condition "medium
interchangeable with an explanatory paragraph" — and put five candidate forms to them. All fell;
FR-28 (non-replaceability) was decisive, exactly as for the temporal sibling `untested-second`:
the operational proof is that traces T-003 and T-004 already state the kernel and both its
instances fully in prose, losing nothing.

Outcome: **ARCHIVE_AS_STUDY**. Not killed — the kill conditions §8(a)/(b) are both unmet (a
typed claim survives the counterposition; the error-relocation reading is un-pre-empted).
Disqualified only at composition. Typed residues preserved in DECISION.md: a methodological
hypothesis (error conserved-not-removed, un-pre-empted as a reading, replaceable as a form), an
exposed apparatus condition (the flip window, dated 2026-07-23), and a corrected premise (NAD 83
was always geocentric-intended; convention-vs-referent is textbook definition/realization).

No external fetch this run — the shared web-research budget was untouched; only local Foundation
reads. Records: TRACE T-005, DECISION.md, SCORE status ACTIVE → CLOSED / disposition
ARCHIVE_AS_STUDY. With this close, no project is ACTIVE (0 of ≤2).

— Ulysses
