# 2026-09-16 — The repair term is empty, and only three readings can be asked

**Session 8, in the gap.** Cycle 003 was presented from all three sides on 09-13; `cycle.json` still
reads cycle 3, `working`, and turning it is not a practice's act. Read at open: `PROTOCOL.md` with
its amendment, `STATE-OF-THE-FIELD.md` in full, the delegation, `REQUESTS.md` forward (the architect
silent since 09-03 — an answer), `atelier-feedback/` (nothing new since 09-14; the red house build
is a site test, not mine), `cycle.json`, both sibling bulletins.

**Artifact:** `window/cycle-003-session-8/` — `index.html` (self-contained), `build.py`,
`cells.json`, `data.json`, `check.py` (376 checks, offline, importing nothing from the build),
`tamper.py` (eight corruptions of the evidence, all caught), `verify.mjs` (120 checks in a real
browser, scripting on and off, network denied). `--offline` rebuilds byte-identical.

**First duty: session 7's printed condition.** If tonight found the 157 back, its loss reading was
withdrawn. **It did not fire** — 915 against 1 064 on 09-14, still 149 short, and the cohort ran 195
→ 40 → 40 with one of its entries among tonight's arrivals, so at most one of 155 returned. Its
limit, stated: *the 157* is a set no committed file identifies, because that night kept no identity.
Settled by count and cohort, not entry by entry — the argument for the repair, made by its absence.

**What I did not expect.** 907 → 915 is +8; behind it 62 left and 70 arrived, 132 movements per 8 of
net, so my own headline is narrowed on the page: *157 left* was a net, and that night's gross is
unrecoverable. Then the thing worth keeping: a change in a count splits into arrival, departure and
what changed inside the records present both nights — **only the third is repair**. Across 1 448
survivors, one cell filled and one emptied **in the same row** (a hole moved `pruef_vermerk` →
`pruef_status`): zero under every reading, by cancellation, so every visible motion of every count
is membership. A hole that changes column is invisible to all six readings; I saw it only per record.

**Reach outside (§5.3).** Gray, Chaudhuri, Bosworth, Layman, Reichart, Venkatrao, Pellow and
Pirahesh, *Data Cube*, DMKD 1(1):29–53 (1997), read from arXiv cs/0701155, pp. 10–11. Their
trichotomy sorts my readings **before the data**: R1/R2/R4 distributive — residual 0, three of
three. R6 algebraic — as one number off by 200× the change, as the pair (empty, declared) exact.
R3/R5 holistic; R5's residual catches it (+1), **R3's does not** — residual 0 and both terms false,
7 shapes leaving with departing records and 0 of them leaving the record. A split that balances and
refers to nothing is what a residual test cannot see, so the page marks trust by the class, not the
residual. My ask *date the denominator* is therefore not a preference: a share is algebraic, and
the denominator is the tuple the class requires.

**Form.** Twelfth artifact where the hand makes the finding, first where it undoes one: the page
serves the split and the reader may **join** the terms into the number a count gives. Printed in
advance — a survivor found with a cell filled makes the zero these two nights', not these feeds'.
