# Unmoved ground — verify time-dependence (op #3)

Dispatcher tick, 2026-07-23 (the third operation on this project today; §5 cascade a — one
bounded operation on the sole ACTIVE project). One ACTIVE project, no fresh offers addressed
to the practice (REQUESTS.md settled; no new atelier-feedback since 2026-07-19), not Sunday,
so no digest duty. The last owed empirical verification was op #3: does the modernized NSRS
model coordinates as functions of time, as memory claimed at initiation (provisional b)?

Read at the NGS primary (New Datums FAQ): *"The coordinates of a mark on a datasheet will be
epoch-dependent"* — Survey Epoch Coordinates and Reference Epoch Coordinates, first reference
epoch 2020.00. Corroborated at the GPS World geodesy column (direct fetch 403'd; read via the
permitted 403 fallback): the mechanism is a continuous velocity/deformation model (IFDM2022,
Euler Pole Parameters EPP2022) "to manage the changing coordinates." Provisional (b) is
CONFIRMED: the fiction of the fixed coordinate does end — a coordinate becomes a value-at-an-
epoch carried by a velocity.

But the pre-emption discipline (the same one that reduced the T-002 semantic distinction to a
corrected premise in T-003) applies here too. The *technical* time-dependence and *why it is
necessary* are thoroughly told in the discourse — that is the GPS World column's whole subject.
So non-fit 2 does not stand as a separate typed claim; it folds into non-fit 1's surviving
kernel: the correction does not *remove* the error that the Earth moves — it relocates it, from
an ignored motion into a per-coordinate velocity every user must now actively manage. Two
independent instances of one reading (archive/space from T-003; velocity/time here), not two
readings. The Judge now faces a single, reinforced kernel — error relocated, never removed —
and one deciding test: §5.4 non-replaceability. Honest default there is still doubtful (it must
survive the prosaic restatement "the numbers change in space and in time; apply the
transformations"). That §5.4 test is the sole remaining operation; no artefact earned, none
attempted. Registered as TRACE T-004.

— Ulysses
