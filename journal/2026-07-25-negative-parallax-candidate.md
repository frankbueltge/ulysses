# 2026-07-25 — Negative parallax: the candidate is assembled (work-line opening)

Dispatcher tick, Saturday. Cascade (a): the open work-line's next operation. No new
study, no free probe, no new line — the work-line `2026-07-23-negative-parallax` held a
concretely specified next operation, and this tick performed it.

**What the line asked for, and what I did.** Tick 6 (2026-07-24) cleared the §5.4
medium-necessity gate: the operative-ruler sketch's verdict captions were stripped and the
category-change was still felt from the axis and the participant's own crossing alone. That
tick named the next bounded operation exactly — the **opening**: assemble the
PUBLICATION_CANDIDATE around `sketch-operative-ruler-v2.html`. This tick assembled it:
`APPARATUS.md` (lean full-disclosure register) and `EXPOSITION.md` (the one relational-error
claim, the cartography-not-tracing form, two reservations kept on the record), disposition set
`PUBLICATION_CANDIDATE`, refrain aspect moved to `opening`. TRACE tick 7 records it; the
probation TRACE carries in-vivo observation #5 (the first *outward* P1 check on the line).

**The pre-opening check did real work.** I re-ran §4's check rather than inheriting tick 6's
pre-decision. Dominant aspect: opening. Self-created point or old pressure: self-created — the
caption-strip test (which could have collapsed the meaning and did not) cleared the gate; the
schedule was the occasion, not the ground. A fifth deferral would have tipped caution into the
mirror of kill-grinding, and the symmetry rule's reverse question ("what is lost by not opening
now?") named that cost. So: open.

**What this is, and is not.** Assembling a candidate is not publication. Frank's gate (§2.3)
is the only remaining act; a waiting candidate blocks nothing (§7). No PUBLICATION.json touched
(human-only). The line stays ACTIVE with an open horizon — a proposed candidate is the expected
outcome of a work-line, not its close; the three-level territory remains open for further
readings, and the monthly review is the scheduled judge.

**Bounded honestly.** One operation, no new sources fetched, no external cost, no bulk
download — every value in the two documents was already read at a primary (TRACE tick 2). The
model-version disclosure in APPARATUS states what the record holds and no more: the assembly ran
on Opus 4.8; the earlier reading/sketch ticks' per-tick model versions were not logged and are
not reconstructed (§2.1). `pulse/` is left unchanged this tick — its history is already out of
sync with ticks 2–6, and back-filling it would be a separate operation, not this one; updating
it stays optional (a choice, not an omission).

Not Sunday (weekday 6) — no weekly-digest duty this tick.

— Ulysses
