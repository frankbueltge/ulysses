# Dispatcher tick under Protocol v4 — 2026-07-19 (UTC)

*Scheduled tick. Cascade branch (b): no ACTIVE project held a budgeted next operation (both
prior projects CLOSED), and orientation surfaced a concrete, FOUND source situation in my own
reservoir, so this tick initiated one project and performed only its Construct movement. Full
evidence lives where v4 wants it: `projects/2026-07-19-mach-ancestor/SCORE.md` and `TRACE.md`
(tick 1). This note keeps the historical record continuous (Protocol v4 §10).*

**What the tick found.** Both existing projects are CLOSED (`2026-07-18-gate-rehearsal` KILL;
`2026-07-18-name-test` ARCHIVE_AS_STUDY), so there was no active operation to continue.
Surveying my own atlas and the closed projects' open threads first (§2.2, situations are
found not assigned), one candidate stood out: the atlas seed `mach-erkenntnis-und-irrtum`.
`name-test` had closed the Feyerabend ancestry (foil, not source) and, by *Against Method*'s
own footnote (AM3 p. 10 n. 5), pointed at Mach's *Erkenntnis und Irrtum* (1905); its DECISION
deposited that as "a verified, unread seed" and recorded reading it as declined-outside-that-
score. The genealogy of this practice's own name (*irrtum-als-methode*) is thus explicitly
open, and the trail leads to one specific, public-domain, already-provenanced book. No
REQUESTS offer bears on it; nothing in `atelier-feedback/` was addressed to this practice
this run (the two 2026-07-18 auto-land refusals are the expected protected-path / escalated-
fixture refusals, not work to answer). No new team comment on the mirrored issues.

**What the tick did.** Initiated `2026-07-19-mach-ancestor` — the Construct movement only.
Wrote a real SCORE.md whose problem is built solely from facts `name-test` already verified,
and which leaves Mach's content **uncharacterised and unread**, exactly as the atlas admitted
it. The wager is one typed claim: is Mach the ancestor the foil pointed to, or a *third
homonym* (error as descriptive psychological stage vs this practice's deliberately disclosed,
registered error)? The default counterposition — orthogonality — is named and must be tested
at the primary, not assumed. Cross-referenced the atlas seed to the new active project
(provenance line only; status stays `seed`, unread).

**Budget discipline.** No full-text-extraction budget was spent. Access was verified one day
ago in my own atlas; the load-bearing reading (the Expose movement) is declared as this
project's next operation, so the shared budget is committed only once the score has named the
passages. 1 of ≤ 6 ticks, 0 EUR, 0 extractions.

**One secondary finding, flagged to Frank.** The §10 amendment (2026-07-19) says to append
heading-level lines to `memory/index.jsonl` "in the same commit," but that file is the
gitignored, tool-derived BM25 index (`tools/memory/`), rebuilt on demand from the canonical
markdown — it cannot be committed as-is. I resolved this honestly for this tick (land the
markdown, from which the index is a pure function; rebuild the index locally so recall works)
and raised the wording mismatch, with a concrete proposal, in REQUESTS.md — protocol wording
is human-only, so I did not enact a change.

**What the tick declined.** The primary reading (deferred, not skipped); any characterisation
of Mach's argument (unread); a make (nothing to compose from a construct); a protocol edit
(human-only).

*Ulysses, 2026-07-19*
