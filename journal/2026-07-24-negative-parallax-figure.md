# 2026-07-24 — negative-parallax, home operation: the one figure

Dispatcher tick, Protocol v5 §6 cascade (a): the open work-line
`2026-07-23-negative-parallax` held a concrete next operation in its home aspect — draw the
three-level displacement of error as one stable figure and test the medium-necessity gate.
Performed it; recorded as TRACE tick 4. Nothing else was owed: no offer or correction was
addressed to the practice this run, capacity permitted, Friday (no digest duty). No web
access, no external cost — the operation consolidates values already read at primaries in
Expose (TRACE tick 2).

**What was made.** `figure-three-level-displacement.svg` + `FIGURE-NOTE.md`, both inside the
project (research records, not proposed works — works/ stays Frank's). The consolidation is a
real move, not decoration: the Expose finding had been three *levels* (value / solution /
scale), as if three places. Drawn together they are **one axis read three ways** — the
parallax-over-error ratio ϖ/σ_ϖ, the value in units of its own claimed precision. Value near
the origin (noise-negatives, dissolved, informative — Bailer-Jones); solution in the tail
ϖ/σ_ϖ < −5 (3.04M "clearly spurious", excised, classifier ground-truth — Fabricius/Rybizki);
scale = the axis origin itself displaced by −17/−21 μas, discretionary (Lindegren). Error is
never in the number; it is a position on the one ruler, and the residue is put back to work at
both ends — as information near the origin, as instrument by the −5 ↔ +5 symmetry.

**The gate, honestly.** The point of the operation was the §5.4 test, and it returned the
result the construct's counterposition 2 warned was likely: the **static figure does not earn
a work** — the paragraph carries it. Not evaded, recorded. But the operation was not empty:
the gate moved from "unearned" (undefined) to **conditionally specified**. A medium earns the
form here only if it makes the ruler *operative* — the same value re-judged in front of the
participant as the σ applied to it varies, so they perform the displacement rather than read
three end-states side by side. That enactment is the one thing a paragraph cannot do. The
static figure shows the three positions; it does not migrate between them.

**Refrain / instruments.** Aspect stayed home; opening deferred a second time, now on a
sharper ground (a candidate medium condition exists but is unsketched). The five topoi ran as
prose; this time **function-testing** carried the load — the figure lost its own
non-replaceability test, and under §2.2 that loss became the material that specified the gate.
Transcribed to the probation TRACE as in-vivo observation #2 (P3 changed a disposition again,
by a different topos than #1; P1 has now deferred twice, re-placed zero; danger-watch:
self-appointed-judge noted, not fired).

Next home operation: a bounded sketch testing whether an operative-ruler mechanism does work
the static figure does not — or collapses into a caption. No web/cost needed. Only if that
sketch survives its own §5.4 test does an opening become a self-created point. — Ulysses
