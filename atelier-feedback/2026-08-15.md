# Build feedback 2026-08-15

The build gate is red. This letter does not say whose defect it is, because that cannot be derived from the log: a file in the site repository can fail on data from yours — a test here asserting over your chronicle is red when a heading or an entry is missing upstream, and the path it names is ours either way. What failed is quoted below; read it and judge. If nothing in it touches your work, there is nothing on your side to correct.

Run: https://github.com/frankbueltge/frankbueltge.de/actions/runs/31875822748

Failing lines, verbatim from the validation log:

```
     × renders a tile for every experiment whose snapshot answers today 10ms
     ✓ never renders an empty or placeholder reading 5ms
     ✓ draws every mark from a series that exists — no decorative shapes 3ms
 FAIL  src/lib/ops/tiles.test.ts > the live dashboard reads the archive > renders a tile for every experiment whose snapshot answers today
AssertionError: redaction has a committed snapshot and must have a tile: expected [ 'foreknown', 'consensus', …(7) ] to include 'redaction'
 ❯ src/lib/ops/tiles.test.ts:16:74
     14|     const ids = tiles.map((t) => t.id)
::error file=/home/runner/work/frankbueltge.de/frankbueltge.de/src/lib/ops/tiles.test.ts,title=src/lib/ops/tiles.test.ts > the live dashboard reads the archive > renders a tile for every experiment whose snapshot answers today,line=16,column=74::AssertionError: redaction has a committed snapshot and must have a tile: expected [ 'foreknown', 'consensus', …(7) ] to include 'redaction'%0A ❯ src/lib/ops/tiles.test.ts:16:74%0A%0A
```

No deploy happened; the last good state stays live.
