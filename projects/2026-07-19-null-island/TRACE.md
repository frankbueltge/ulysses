# Trace — 2026-07-19-null-island

## Tick 1 — 2026-07-19 — initiation (construct)

**Movement performed:** Construct (Protocol v4 §5.1). This dispatcher tick's single bounded
operation was to initiate the project: find the situation in the world, verify its
load-bearing facts, write a real SCORE.md, run the mandate self-check honestly. No other
movement was performed.

**How this situation was found (in the world, not assigned).** Orientation this run found
cascade (a) inert: all three existing projects are CLOSED (`gate-rehearsal` KILL;
`name-test` and `mach-ancestor` ARCHIVE_AS_STUDY), so no ACTIVE project held a budgeted next
operation. The `mach-ancestor` DECISION (2026-07-19, team direction) closed the
self-genealogy line and instructed that "the next tick initiates from the world" under the
same-day §2.2 amendment ("situations face the world"). So orientation deliberately did **not**
mine the practice's own record or its theory-atlas for a fourth self-referential topic;
it reached outward with the research tools. `error as method` was carried only as a **lens**,
not as the subject: the subject had to be a concrete object out there.

The situation surfaced by web research and was then verified, not assumed: **Null Island** —
the coordinate `0°N 0°E` where geospatial pipelines default their "no data" case, and where a
deliberate 1 m² decoy was drawn into the public-domain Natural Earth dataset to *catch* errors
by staying invisible. It is concrete, public, inspectable, and it is a working method built
out of error in the world's own infrastructure — the near-negative of this practice's
disclosed-error method. No REQUESTS offer bears on it; nothing in `atelier-feedback/` was
addressed to this practice this run (the two 2026-07-18 auto-land refusals remain the expected
protected-path / escalated-fixture refusals). No new team comment on the mirrored issues.

**What the construct rests on — verified this run.** Two tool calls, both landing on
retrievable sources:
- WebSearch (Null Island / Natural Earth / station 13010) — confirmed the shape of the
  situation and surfaced the primaries.
- One WebFetch of the Wikipedia summary, **cross-checked against** the Library of Congress
  blog (Tim St. Onge, 2016-04-22, retrievable) — yielding the load-bearing specifics now in
  §1: the `0,0` sentinel default; the Natural Earth "1 metre square island, scale rank 100 —
  should never be shown"; the accidental sediment (Bellingcat 2018, Strava, aggregate only);
  and buoy Station 13010 "Soul" (PIRATA/ATLAS, ~3.8 m, **decommissioned March 2021**).

**What the construct deliberately does NOT rest on.** The raw primary technical records —
the Natural Earth shapefile attribute itself, the NDBC/PIRATA station 13010 page, and the
OpenStreetMap node at `0,0` — are **not yet inspected at source**. Every figure taken from
the Wikipedia summary (buoy dimensions, network membership, decommission date, the exact
"never shown" attribute) is marked **provisional** in the score until read at its primary.
That reading is the Expose movement (§5.2) and is declared as the next operation. This keeps
the S37 discipline structural: the encyclopaedia is used as an index to primaries, never as a
primary itself.

**Budget discipline — why no full-text-extraction this tick.** The shared full-text-extraction
budget is finite (Standing Delegation v2 §2). A Construct needs only enough verified fact to
state a real non-fit, which two ordinary web calls supplied. Spending the extraction budget to
*read* the primaries before the score has named which records are load-bearing would violate
the §6 discipline. So the primary reads are the next budgeted operation, and **0 EUR / 0
extractions** were spent here.

**One deliberate abstention.** The atlas (`atlas/atlas.json`) was **not** edited. It is, by
the §2.2 amendment's own diagnosis, a shelf of the discipline's *own theory* — the instrument
that amplified the inward turn. Forcing an outward data-object (a coordinate, a shapefile
record) into a theory-atlas would distort the instrument and re-import the very bias just
corrected. The situation is recorded where it belongs — this project and the journal, from
which recall is indexed. Updating the atlas is optional (Protocol v4 §5 / Standing Delegation),
and here the honest choice was not to.

**Declined this tick:** the primary reads (deferred as the Expose operation, not skipped); any
firm claim about the three regimes being one or three (that is what Expose must decide); a make
(nothing to compose from a construct); an atlas edit (would distort the instrument).

**Budget consumed:** 1 of ≤ 6 ticks; 0 of ≤ 21 days; 0 EUR; 0 full-text extractions;
1 WebSearch + 1 WebFetch (ordinary, not the shared extraction budget).

**Next operation (declared):** the Expose movement — read the three primary technical records
(Natural Earth Null Island shapefile attribute; NDBC/PIRATA station 13010; the OSM node at
`0,0`), confirm or withdraw each provisional figure, and test the central distinction: are the
null-value, the accidental sediment and the deliberate invisible decoy one sentinel convention
under three descriptions, or three genuinely different operations of error sharing one address?
Also a bounded, sourced pass on the deliberate-error lineage (Agloe and copyright traps). Then
form the one typed claim and set disposition.
